const Footer = () => {
  const footerStyle = {
    padding: '2rem',
    textAlign: 'center',
    background: '#333',
    color: 'white',
    marginTop: 'auto'
  };

  return (
    <footer style={footerStyle}>
      <p>&copy; footer</p>
    </footer>
  );
};

export default Footer;